# Supabase Public Configuration (Safe for client side)
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...

# Note: The Supabase Service Role Key must NEVER be prefixed with NEXT_PUBLIC_
# and must NOT be exposed in client code. Only use it on protected server endpoints if needed.
# SUPABASE_SERVICE_ROLE_KEY=your-service-role-secret
